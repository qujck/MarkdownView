@model MarkdownView.Models.Page

@{
    ViewBag.Title = Model.Name;   
}

<div class="container">
    <div class="row">
        <div class="col-md-10">
            <img src='@Url.Content("~/content/logo.png")' class='pull-right' />
            @Html.Raw(Model.Markdown)
        </div>
    </div>
</div>